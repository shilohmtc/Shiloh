const { escapeHtml, workspaceShellStyles, renderWorkspaceNavigation } = require('./workspaceShell');

function styles() {
  return `:root{color-scheme:light;--ink:#20322b;--muted:#5f7168;--paper:#f4f3ed;--panel:#fffdf9;--line:#dce3dd;--line-strong:#c6d1c9;--leaf:#294c3c;--leaf-soft:#e7eee9;--warn:#79522f;--warn-soft:#f5eee5;--danger:#87413b;--danger-soft:#f7e9e7;--shadow:0 8px 28px rgba(32,50,43,.07)}*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.shell{max-width:940px;margin:0 auto;padding:24px 18px 44px}.topbar{display:flex;align-items:flex-end;justify-content:space-between;gap:16px;margin-bottom:18px}.brand h1{margin:0;font-size:1.65rem}.brand p{margin:6px 0 0;color:var(--muted);line-height:1.5}.button{display:inline-flex;align-items:center;justify-content:center;min-height:44px;border:1px solid var(--line-strong);border-radius:999px;padding:9px 14px;background:#fff;color:var(--ink);font:inherit;font-size:.82rem;font-weight:780;text-decoration:none;cursor:pointer}.button.primary{background:var(--leaf);border-color:var(--leaf);color:#fff}.staff-access-list{display:grid;gap:10px}.person-card{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;align-items:center;padding:16px;border:1px solid var(--line);border-radius:16px;background:var(--panel);box-shadow:var(--shadow);text-decoration:none}.person-card:hover{border-color:#9eb1a5}.person-name{font-size:1rem;font-weight:850}.person-summary{margin:4px 0 0;color:var(--muted);font-size:.8rem;line-height:1.45}.badges{display:flex;align-items:center;gap:7px;flex-wrap:wrap;margin-top:8px}.badge{display:inline-flex;border-radius:999px;padding:5px 9px;background:var(--leaf-soft);font-size:.7rem;font-weight:800}.badge.off{background:var(--warn-soft);color:var(--warn)}.arrow{font-size:1.4rem;color:var(--muted)}.detail-card,.restriction-card{padding:18px;border:1px solid var(--line);border-radius:17px;background:var(--panel);box-shadow:var(--shadow);margin-bottom:12px}.detail-head{display:flex;align-items:flex-start;justify-content:space-between;gap:14px}.detail-head h2{margin:2px 0 5px;font-size:1.2rem}.eyebrow{font-size:.69rem;letter-spacing:.11em;text-transform:uppercase;font-weight:850;color:var(--muted)}.profile-copy{margin:0;color:var(--muted);line-height:1.5;font-size:.84rem}.switch-list{display:grid;gap:9px;margin-top:16px}.switch-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:14px;align-items:center;padding:13px;border:1px solid var(--line);border-radius:13px;background:#fff}.switch-copy strong{display:block;font-size:.88rem}.switch-copy span{display:block;margin-top:4px;color:var(--muted);font-size:.74rem;line-height:1.4}.switch{display:inline-flex;align-items:center;gap:8px;min-width:92px;min-height:46px;border:1px solid var(--line-strong);border-radius:999px;padding:5px 8px 5px 6px;background:#fff;color:var(--muted);font:inherit;font-size:.76rem;font-weight:850;cursor:pointer;touch-action:manipulation}.switch-track{position:relative;width:38px;height:22px;flex:0 0 38px;border-radius:999px;background:#b9c3bd;transition:background .15s ease}.switch-track::after{content:'';position:absolute;top:3px;left:3px;width:16px;height:16px;border-radius:50%;background:#fff;box-shadow:0 1px 3px rgba(0,0,0,.22);transition:transform .15s ease}.switch[aria-checked="true"]{color:var(--leaf);border-color:#a6b9ae}.switch[aria-checked="true"] .switch-track{background:var(--leaf)}.switch[aria-checked="true"] .switch-track::after{transform:translateX(16px)}.switch:focus-visible,.button:focus-visible{outline:3px solid #9ebaaa;outline-offset:2px}.switch:disabled,.button:disabled{opacity:.55;cursor:wait}.restriction-card{background:var(--leaf-soft);box-shadow:none}.restriction-card h3{margin:0 0 8px;font-size:.95rem}.restriction-card ul{margin:0;padding-left:20px}.restriction-card li{margin:6px 0;font-size:.8rem;line-height:1.4}.actions{display:flex;gap:9px;flex-wrap:wrap;margin-top:14px}.status{min-height:1.4em;margin:8px 0 0;color:var(--muted);font-size:.78rem}.status[data-tone="error"]{color:var(--danger)}.status[data-tone="working"]{color:var(--warn)}.protected{padding:14px;border:1px solid var(--line);border-radius:13px;background:#fff;color:var(--muted);font-size:.8rem;line-height:1.5}@media(max-width:700px){.shell{padding:16px 12px 30px}.topbar{align-items:flex-start;flex-direction:column}.topbar .button{width:100%}.person-card{padding:14px}.switch-row{grid-template-columns:1fr;gap:10px}.switch{width:100%;justify-content:space-between;min-height:48px}.detail-head{display:grid}.actions{display:grid}.actions .button{width:100%}}`;
}

function shell(model, body, { detail = false } = {}) {
  const displayName = model?.authority?.displayName || '';
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Staff access — Shiloh Workspace</title><style>${workspaceShellStyles()}${styles()}</style><script src="/calendar/team/staff-access/client.js" defer></script></head><body data-workspace-staff-access="true"><div class="workspace-frame">${renderWorkspaceNavigation({ active: 'staff', displayName, dashboardHref: '/calendar/workspace', calendarHref: '/calendar/read-only', clientsHref: '/calendar/clients', messagesHref: '/calendar/messages', staffHref: '/calendar/team/staff-access', servicesHref: '/calendar/services', reportsHref: '/calendar/reports', clinicHoursHref: '/calendar/clinic-hours' })}<main class="workspace-main"><div class="shell"><header class="topbar"><div class="brand"><h1>Staff access</h1><p>${detail ? 'Choose what this person can do. Protected clinic boundaries stay locked.' : 'Simple access profiles for the people who use Shiloh Workspace.'}</p></div><a class="button" href="${detail ? '/calendar/team/staff-access' : '/calendar/team'}">${detail ? 'Back to Staff access' : 'Staff profiles'}</a></header>${body}</div></main></div></body></html>`;
}

function renderStaffAccessPage(model) {
  const cards = (model.people || []).map((person) => `<a class="person-card" href="/calendar/team/staff-access/${person.id}"><div><div class="person-name">${escapeHtml(person.displayName)}</div><p class="person-summary">${escapeHtml(person.profileSummary)}</p><div class="badges"><span class="badge">${escapeHtml(person.profileLabel)}</span><span class="badge ${person.active ? '' : 'off'}">${person.active ? 'Access active' : 'Access off'}</span>${person.editable ? '' : '<span class="badge off">Protected</span>'}</div></div><span class="arrow" aria-hidden="true">›</span></a>`).join('');
  return shell(model, `<section class="staff-access-list" aria-label="Staff access">${cards || '<div class="protected">No staff access profiles are available.</div>'}</section>`);
}

function renderToggle(toggle) {
  return `<div class="switch-row"><div class="switch-copy"><strong>${escapeHtml(toggle.label)}</strong><span>${escapeHtml(toggle.description)}</span></div><button class="switch" type="button" role="switch" aria-label="${escapeHtml(toggle.label)}" aria-checked="${toggle.on ? 'true' : 'false'}" data-access-toggle="${escapeHtml(toggle.key)}"><span class="switch-track" aria-hidden="true"></span><span data-switch-state>${toggle.on ? 'On' : 'Off'}</span></button></div>`;
}

function renderStaffAccessDetail(model) {
  const person = model.person;
  if (!person) return shell(model, '<div class="protected">Staff access was not found.</div>', { detail: true });
  const restrictions = (person.protectedRestrictions || []).map((item) => `<li>${escapeHtml(item)}</li>`).join('');
  const controls = person.editable
    ? `<div class="switch-list">${(person.toggles || []).map(renderToggle).join('')}</div><div class="actions"><button class="button" type="button" data-restore-profile>Restore recommended settings</button></div><p class="status" role="status" aria-live="polite" data-access-status></p>`
    : '<div class="protected">This access is protected and is managed through the administrator/owner rules.</div>';
  return shell(model, `<section class="detail-card" data-access-person="${person.id}" data-access-profile="${escapeHtml(person.profileKey || '')}" data-access-revision="${escapeHtml(person.revision || '')}"><div class="detail-head"><div><span class="eyebrow">${escapeHtml(person.profileLabel)}</span><h2>${escapeHtml(person.displayName)}</h2><p class="profile-copy">${escapeHtml(person.profileSummary)}</p></div><span class="badge ${person.active ? '' : 'off'}">${person.active ? 'Access active' : 'Access off'}</span></div>${controls}</section>${restrictions ? `<section class="restriction-card"><h3>Protected boundaries</h3><ul>${restrictions}</ul></section>` : ''}`, { detail: true });
}

function clientScript() {
  return `(()=>{'use strict';const card=document.querySelector('[data-access-person]');if(!card)return;const status=document.querySelector('[data-access-status]');const buttons=[...document.querySelectorAll('[data-access-toggle]')];const restore=document.querySelector('[data-restore-profile]');const personId=card.dataset.accessPerson;function msg(text,tone=''){if(!status)return;status.textContent=text||'';if(tone)status.dataset.tone=tone;else delete status.dataset.tone}function rid(){return 'staffaccess_'+Date.now().toString(36)+Math.random().toString(36).slice(2,8)}async function csrf(){const r=await fetch('/calendar/staff-auth/csrf',{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json'},body:'{}'});const b=await r.json().catch(()=>({}));if(!r.ok||!b.csrfToken)throw Error(b.error||'Secure session expired.');return b.csrfToken}async function post(path,payload){const r=await fetch(path,{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json','x-shiloh-csrf-token':await csrf()},body:JSON.stringify(payload)});const b=await r.json().catch(()=>({}));if(!r.ok)throw Error(b.error||'Nothing was changed.');return b}function applyPerson(person){card.dataset.accessRevision=person.revision;for(const button of buttons){const state=(person.toggles||[]).find(t=>t.key===button.dataset.accessToggle);if(!state)continue;button.setAttribute('aria-checked',state.on?'true':'false');const text=button.querySelector('[data-switch-state]');if(text)text.textContent=state.on?'On':'Off'}}for(const button of buttons){button.addEventListener('click',async()=>{const before=button.getAttribute('aria-checked')==='true',next=!before;buttons.forEach(b=>b.disabled=true);if(restore)restore.disabled=true;button.setAttribute('aria-checked',next?'true':'false');const text=button.querySelector('[data-switch-state]');if(text)text.textContent=next?'On':'Off';msg('Saving…','working');try{const result=await post('/calendar/team/staff-access/'+encodeURIComponent(personId)+'/toggle',{expectedRevision:card.dataset.accessRevision,requestId:rid(),toggle:button.dataset.accessToggle,on:next});applyPerson(result.person);msg('Saved.')}catch(error){button.setAttribute('aria-checked',before?'true':'false');if(text)text.textContent=before?'On':'Off';msg(error.message,'error')}finally{buttons.forEach(b=>b.disabled=false);if(restore)restore.disabled=false}})}if(restore)restore.addEventListener('click',async()=>{if(!confirm('Restore the recommended settings for this access profile?'))return;buttons.forEach(b=>b.disabled=true);restore.disabled=true;msg('Saving…','working');try{const result=await post('/calendar/team/staff-access/'+encodeURIComponent(personId)+'/profile',{expectedRevision:card.dataset.accessRevision,requestId:rid(),profile:card.dataset.accessProfile});applyPerson(result.person);msg('Recommended settings restored.')}catch(error){msg(error.message,'error')}finally{buttons.forEach(b=>b.disabled=false);restore.disabled=false}})})();`;
}

module.exports = {
  styles,
  renderStaffAccessPage,
  renderStaffAccessDetail,
  clientScript,
};
