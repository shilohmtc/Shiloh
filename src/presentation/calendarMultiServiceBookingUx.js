function safeJson(value) {
  return JSON.stringify(value || {}).replace(/</g, '\\u003c');
}

function escapeHtml(value = '') {
  return String(value).replace(/[&<>"']/g, character => ({
    '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;',
  }[character]));
}

function treatmentOptions(services = []) {
  return services.map(service => `<option value="${Number(service.id)}">${escapeHtml(service.name)}</option>`).join('');
}

function treatmentCard(position, services = []) {
  return `<article class="treatment-card" data-treatment="${position}">
    <header><div><span class="eyebrow">Treatment ${position}</span><strong data-treatment-title>Choose treatment</strong></div><button class="text-button remove" type="button" data-remove-treatment aria-label="Remove Treatment ${position}">Remove</button></header>
    <div class="treatment-fields">
      <label>Treatment<select data-service><option value="">Choose treatment</option>${treatmentOptions(services)}</select></label>
      <label>Practitioner<select data-staff disabled><option value="">Choose treatment first</option></select></label>
      <label>Start time<input data-start-time type="time" step="300" required></label>
    </div>
    <p class="treatment-summary" data-treatment-summary>Duration and price will appear here.</p>
  </article>`;
}

function styles() {
  return `:root{color-scheme:light;--ink:#20322b;--muted:#637169;--paper:#f6f3ed;--panel:#fffdf9;--line:#d9e2db;--leaf:#315744;--soft:#e7f0ea;--gold:#987447;--danger:#8a3f3f;--danger-soft:#f8eaea}*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font-family:Inter,system-ui,sans-serif}.shell{max-width:1020px;margin:auto;padding:20px 16px 48px}.topbar,.treatment-card header,.treatment-toolbar{display:flex;align-items:center;justify-content:space-between;gap:14px}.topbar{margin-bottom:14px}.topbar h1{margin:2px 0;font-family:Georgia,serif;font-size:1.7rem;font-weight:500}.topbar p,.hint{margin:4px 0;color:var(--muted);line-height:1.45}.eyebrow{font-size:.7rem;letter-spacing:.11em;text-transform:uppercase;font-weight:850;color:var(--gold)}.panel,.treatment-card{background:var(--panel);border:1px solid var(--line);border-radius:17px;box-shadow:0 5px 18px rgba(32,50,43,.05)}.panel{padding:17px;margin-bottom:14px}.intro{border-left:5px solid var(--leaf);background:var(--soft)}.client-search{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px}.client-results{display:grid;gap:7px;margin-top:9px}.client-result{display:flex;justify-content:space-between;gap:8px;width:100%;min-height:44px;padding:9px 11px;border:1px solid var(--line);border-radius:10px;background:#fff;text-align:left;font:inherit}.client-result small{display:block;color:var(--muted)}.selected-client{margin-top:10px;padding:11px;border:1px solid #b8cdbf;border-radius:11px;background:var(--soft);font-weight:800}.session-grid,.treatment-fields{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.session-grid{grid-template-columns:minmax(0,1fr)}label{display:grid;gap:5px;font-size:.76rem;font-weight:800}input,select,textarea{display:block;width:100%;min-width:0;min-height:44px;border:1px solid var(--line);border-radius:10px;padding:9px 10px;background:#fff;color:var(--ink);font:inherit}.treatment-toolbar{margin:18px 0 10px}.treatment-toolbar p{margin:3px 0}.treatment-list{display:grid;gap:12px}.treatment-card{padding:15px}.treatment-card header{margin-bottom:12px}.treatment-card header>div{display:grid;gap:3px}.treatment-card header strong{font-size:1.03rem}.treatment-summary{margin:10px 0 0;color:var(--muted);font-size:.82rem}.button{display:inline-flex;align-items:center;justify-content:center;min-height:44px;border:1px solid var(--leaf);border-radius:999px;padding:9px 15px;background:var(--leaf);color:#fff;font:inherit;font-weight:800;cursor:pointer;text-decoration:none}.button.secondary{background:#fff;color:var(--leaf)}.button:disabled{opacity:.5}.text-button{display:inline-flex;align-items:center;min-height:44px;border:0;background:transparent;color:var(--leaf);font:inherit;font-size:.76rem;font-weight:800;text-decoration:underline;cursor:pointer}.text-button.remove{color:var(--danger)}.status{margin-top:12px;padding:11px;border:1px solid var(--line);border-radius:11px;background:#fff;white-space:pre-line}.status.ready{background:var(--soft);color:var(--leaf)}.status.error{background:var(--danger-soft);color:var(--danger)}.actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.actions.final{margin-top:14px}.review{display:grid;gap:7px}.review-row{display:flex;justify-content:space-between;gap:16px;padding:8px 0;border-bottom:1px solid var(--line)}.review-row span{color:var(--muted)}.review-treatment{display:grid;gap:3px;padding:12px;border:1px solid var(--line);border-radius:12px;background:#fff}.review-treatment strong{font-size:.92rem}.review-treatment small{color:var(--muted)}[hidden]{display:none!important}@media(max-width:700px){.shell{padding:10px 9px 32px}.topbar{align-items:flex-start;flex-direction:column}.topbar .button{width:100%}.client-search{grid-template-columns:minmax(0,1fr) 76px}.treatment-fields{grid-template-columns:1fr}.panel,.treatment-card{padding:13px}.actions.final{position:sticky;bottom:8px;z-index:4;padding:8px;border:1px solid var(--line);border-radius:14px;background:rgba(255,253,249,.96);box-shadow:0 8px 22px rgba(32,50,43,.13)}.actions.final .button{width:100%}.review-row{align-items:flex-start;flex-direction:column;gap:3px}input[type=date],input[type=time]{-webkit-appearance:none;appearance:none;font-size:16px}}`;
}

function renderCalendarMultiServiceBookingPage({ options = {}, prefill = {}, clientScriptPath = '/calendar/book/multiple/client.js' } = {}) {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Multiple treatments · Shiloh</title><style>${styles()}.eyebrow{color:#8c693f}.intro .hint{color:#5c6a62}</style><script src="${escapeHtml(clientScriptPath)}" defer></script></head><body><div class="shell">
    <header class="topbar"><div><span class="eyebrow">Calendar</span><h1>Book multiple treatments</h1><p>One client, one linked visit, with the right time and practitioner for every treatment.</p></div><a class="button secondary" href="/calendar/read-only${prefill.date ? `?view=week&amp;date=${escapeHtml(prefill.date)}&amp;staff=all` : ''}">← Back to Calendar</a></header>
    <main>
      <section class="panel intro"><strong>Everything stays together.</strong><div class="hint">Shiloh checks each treatment separately, then creates the whole visit at once. The client receives one combined confirmation and one booking total.</div></section>
      <section class="panel"><span class="eyebrow">1 · Client</span><h2>Who is visiting?</h2><div class="client-search"><input data-client-search-input type="search" autocomplete="off" placeholder="Search name or mobile" aria-label="Find client"><button class="button secondary" type="button" data-search-client>Find</button></div><div class="client-results" data-client-results></div><div class="selected-client" data-selected-client hidden></div></section>
      <section class="panel"><span class="eyebrow">2 · Visit</span><div class="session-grid"><label>Date<input id="multi-date" type="date" value="${escapeHtml(prefill.date || '')}" required></label></div></section>
      <div class="treatment-toolbar"><div><span class="eyebrow">3 · Treatments</span><p class="hint"><span data-treatment-count>2</span> treatments in this visit</p></div><button class="button secondary" type="button" data-add-treatment>+ Add treatment</button></div>
      <div class="treatment-list" data-treatment-list>${treatmentCard(1, options.services)}${treatmentCard(2, options.services)}</div>
      <section class="panel"><label>Appointment note <span class="hint">Optional</span><textarea data-notes maxlength="2000" rows="3" placeholder="Anything the team should know about this visit"></textarea></label><div class="actions final"><button class="button" type="button" data-review-multiple>Review linked visit</button></div><div class="status" role="status" aria-live="polite" data-multiple-status>Select one client and complete at least two treatments.</div></section>
      <section class="panel" data-multiple-review hidden><span class="eyebrow">Final check</span><h2>Review linked visit</h2><div class="review" data-review-rows></div><p class="hint">One final tap rechecks every treatment, practitioner, price and calendar before anything is created.</p><div class="actions"><button class="button" type="button" data-confirm-multiple>Create linked visit</button><button class="button secondary" type="button" data-discard-multiple>Edit details</button></div></section>
    </main>
    <script type="application/json" id="multi-options">${safeJson({ ...options, prefill })}</script><template id="treatment-template">${treatmentCard('__POSITION__', options.services)}</template>
  </div></body></html>`;
}

function calendarMultiServiceBookingClientScript() {
  return `(function(){'use strict';var API='/calendar/book/multiple',AUTH='/calendar/staff-auth',MIN=2,MAX=10,prepared=false,selectedClient=null,OPTIONS=JSON.parse(document.getElementById('multi-options').textContent||'{}');function q(s,r){return(r||document).querySelector(s)}function qa(s,r){return Array.prototype.slice.call((r||document).querySelectorAll(s))}function json(r){return r.json().catch(function(){return{}})}function cards(){return qa('[data-treatment]')}function money(v){return new Intl.NumberFormat('en-ZA',{style:'currency',currency:'ZAR'}).format(Number(v||0))}function status(m,t){var n=q('[data-multiple-status]');n.textContent=m;n.className='status'+(t?' '+t:'');if(t==='error'){n.tabIndex=-1;n.focus({preventScroll:true});n.scrollIntoView({block:'nearest'})}}async function csrf(){var r=await fetch(AUTH+'/csrf',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:'{}'}),b=await json(r);if(!r.ok||!b.csrfToken)throw new Error('SESSION');return b.csrfToken}async function post(path,payload,secure){var h={'Content-Type':'application/json','Accept':'application/json'};if(secure)h['x-shiloh-csrf-token']=await csrf();return fetch(API+path,{method:'POST',credentials:'same-origin',headers:h,body:JSON.stringify(payload||{})})}function clearReview(){prepared=false;q('[data-multiple-review]').hidden=true}function selectedService(card){var id=Number(q('[data-service]',card).value);return(OPTIONS.services||[]).find(function(item){return Number(item.id)===id})||null}function syncStaff(card){var service=selectedService(card),select=q('[data-staff]',card),current=select.value,allowed=new Set((service&&service.staffIds||[]).map(Number));select.textContent='';var first=document.createElement('option');first.value='';first.textContent=service?'Choose practitioner':'Choose treatment first';select.appendChild(first);(OPTIONS.staff||[]).forEach(function(person){if(!allowed.has(Number(person.id)))return;var option=document.createElement('option');option.value=String(person.id);option.textContent=person.displayName;select.appendChild(option)});if(Array.from(select.options).some(function(option){return option.value===current}))select.value=current;select.disabled=!service;q('[data-treatment-title]',card).textContent=service?service.name:'Choose treatment';q('[data-treatment-summary]',card).textContent=service?service.durationMinutes+' minutes · '+money(service.price):'Duration and price will appear here.'}function minutes(value){var p=String(value||'').split(':').map(Number);return p.length===2&&p.every(Number.isFinite)?p[0]*60+p[1]:null}function clock(value){value=((value%1440)+1440)%1440;return String(Math.floor(value/60)).padStart(2,'0')+':'+String(value%60).padStart(2,'0')}function suggestFollowingTimes(){var rows=cards();for(var i=1;i<rows.length;i++){var previous=rows[i-1],current=rows[i],previousStart=minutes(q('[data-start-time]',previous).value),service=selectedService(previous),input=q('[data-start-time]',current);if(!input.value&&previousStart!==null&&service)input.value=clock(previousStart+Number(service.durationMinutes||0))}}function renumber(){cards().forEach(function(card,index){var p=index+1;card.dataset.treatment=String(p);q('.eyebrow',card).textContent='Treatment '+p;q('[data-remove-treatment]',card).setAttribute('aria-label','Remove Treatment '+p);q('[data-remove-treatment]',card).hidden=cards().length<=MIN});q('[data-treatment-count]').textContent=String(cards().length);q('[data-add-treatment]').disabled=cards().length>=MAX}function addTreatment(){if(cards().length>=MAX)return;var p=cards().length+1,html=q('#treatment-template').innerHTML.replace(/__POSITION__/g,String(p)),box=document.createElement('div');box.innerHTML=html.trim();var card=box.firstElementChild;q('[data-treatment-list]').appendChild(card);syncStaff(card);renumber();suggestFollowingTimes();clearReview();status('Treatment '+p+' added.')}function removeTreatment(card){if(cards().length<=MIN)return;card.remove();renumber();clearReview();status('Treatment removed.')}function chooseClient(client){selectedClient=client;q('[data-selected-client]').hidden=false;q('[data-selected-client]').textContent=client.displayName+(client.contactHint?' · '+client.contactHint:'');q('[data-client-results]').textContent='';clearReview();status('Client selected. Now complete the treatments.')}async function search(){var term=q('[data-client-search-input]').value.trim();if(term.length<2){status('Enter at least two characters to find a client.','error');return}status('Finding clients…');var r=await post('/client-search',{query:term},false),b=await json(r),host=q('[data-client-results]');host.textContent='';if(!r.ok){status(b.error||'Client search is unavailable.','error');return}if(!b.clients||!b.clients.length){selectedClient=null;q('[data-selected-client]').hidden=true;status('No client matched. Create the client first, then return to book the linked visit.','error');return}b.clients.forEach(function(client){var button=document.createElement('button');button.type='button';button.className='client-result';var left=document.createElement('span');left.textContent=client.displayName;var small=document.createElement('small');small.textContent=[client.profileStatus,client.contactHint].filter(Boolean).join(' • ');left.appendChild(small);var pick=document.createElement('strong');pick.textContent='Select';button.append(left,pick);button.addEventListener('click',function(){chooseClient(client)});host.appendChild(button)});status('Choose the correct client.')}function payload(){return{clientId:selectedClient&&selectedClient.id,date:q('#multi-date').value,notes:q('[data-notes]').value,treatments:cards().map(function(card){return{serviceId:q('[data-service]',card).value,staffId:q('[data-staff]',card).value,startTime:q('[data-start-time]',card).value}})}}function localError(p){if(!p.clientId)return'Find and select one client.';if(!p.date)return'Choose the visit date.';if(p.treatments.length<MIN||p.treatments.length>MAX)return'Add between 2 and 10 treatments.';if(p.treatments.some(function(item){return!item.serviceId}))return'Choose every treatment.';if(p.treatments.some(function(item){return!item.staffId}))return'Choose a practitioner for every treatment.';if(p.treatments.some(function(item){return!/^([01]\\d|2[0-3]):[0-5]\\d$/.test(item.startTime)}))return'Choose a valid start time for every treatment.';for(var left=0;left<p.treatments.length;left++){for(var right=left+1;right<p.treatments.length;right++){if(p.treatments[left].staffId!==p.treatments[right].staffId)continue;var lstart=minutes(p.treatments[left].startTime),rstart=minutes(p.treatments[right].startTime),lduration=Number(selectedService(cards()[left]).durationMinutes||0),rduration=Number(selectedService(cards()[right]).durationMinutes||0);if(lstart<rstart+rduration&&rstart<lstart+lduration)return'The same practitioner cannot provide overlapping treatments.'}}return null}function reviewTreatment(item){var box=document.createElement('div');box.className='review-treatment';var title=document.createElement('strong');title.textContent='Treatment '+item.position+' · '+item.service.name;var detail=document.createElement('small');detail.textContent=item.practitioner.displayName+' · '+new Intl.DateTimeFormat('en-ZA',{timeZone:'Africa/Johannesburg',hour:'2-digit',minute:'2-digit',hour12:false}).format(new Date(item.startsAt))+'–'+new Intl.DateTimeFormat('en-ZA',{timeZone:'Africa/Johannesburg',hour:'2-digit',minute:'2-digit',hour12:false}).format(new Date(item.endsAt))+' · '+money(item.service.price);box.append(title,detail);return box}function row(label,value){var line=document.createElement('div');line.className='review-row';var l=document.createElement('span');l.textContent=label,v=document.createElement('strong');v.textContent=value;line.append(l,v);return line}async function review(){clearReview();var p=payload(),error=localError(p);if(error){status(error,'error');return}status('Checking every treatment and calendar…');var r=await post('/prepare',p,true),b=await json(r);if(!r.ok){status(b.error||'This linked visit cannot be prepared.','error');return}var host=q('[data-review-rows]');host.textContent='';host.append(row('Client',b.review.client.displayName));b.review.assignments.forEach(function(item){host.append(reviewTreatment(item))});host.append(row('Booking total',money(b.review.subtotal)));q('[data-multiple-review]').hidden=false;prepared=true;status('Review ready. One final tap creates the whole linked visit.','ready');q('[data-multiple-review]').scrollIntoView({block:'start'})}async function discard(){if(prepared)await post('/discard',{},true);clearReview();status('Review discarded. Nothing was created.')}async function confirm(){if(!prepared)return;status('Creating the linked visit…');var r=await post('/confirm',{},true),b=await json(r);if(!r.ok){clearReview();status(b.error||'Nothing was created. Review the linked visit again.','error');return}prepared=false;var delivery=b.customerConfirmation||{};if(delivery.status!=='sent'){status('Linked visit created. The client confirmation is queued for retry; please confirm manually if needed.','error');return}status('Linked visit created and one combined confirmation sent.','ready');window.location.assign('/calendar/read-only?'+new URLSearchParams({view:'week',date:q('#multi-date').value,staff:'all'}).toString())}document.addEventListener('click',function(e){if(e.target.closest('[data-add-treatment]'))return addTreatment();var card=e.target.closest('[data-treatment]');if(card&&e.target.closest('[data-remove-treatment]'))return removeTreatment(card)});document.addEventListener('change',function(e){var card=e.target.closest('[data-treatment]');if(card&&e.target.matches('[data-service]'))syncStaff(card);suggestFollowingTimes();clearReview()});q('[data-search-client]').addEventListener('click',search);q('[data-client-search-input]').addEventListener('keydown',function(e){if(e.key==='Enter'){e.preventDefault();search()}});q('[data-review-multiple]').addEventListener('click',review);q('[data-discard-multiple]').addEventListener('click',discard);q('[data-confirm-multiple]').addEventListener('click',confirm);cards().forEach(syncStaff);renumber()})();`;
}

module.exports = {
  renderCalendarMultiServiceBookingPage,
  calendarMultiServiceBookingClientScript,
  treatmentCard,
  escapeHtml,
};
